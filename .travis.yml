language: python
os:
  - linux
python:
  - 3.6 # only test on 3.6 and save notebook outputs on 3.6
sudo: false
install:
  - pip install -r requirements.txt
  - pip install git+https://github.com/simpeg/discretize/tree/feat/vtk.git
script:
  - cd examples
  - sh ./test.sh
notifications:
  email:
    recipients:
    - info@pvgeo.org
    on_success: never
    on_failure: never
